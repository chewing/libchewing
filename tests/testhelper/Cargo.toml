[package]
name = "chewing_testhelper"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
chewing = { version = "0.8.0-beta.3", path = "../.." }

[features]
sqlite = ["chewing/sqlite"]
