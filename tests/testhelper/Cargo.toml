[package]
name = "chewing-testhelper"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
chewing = { version = "0.6.0-alpha.2", path = "../..", features = ["capi"] }

[features]
sqlite = ["chewing/sqlite"]
