[package]
name = "chewing_testhelper"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
chewing = { version = "0.9.1", path = "../.." }

[features]
sqlite = ["chewing/sqlite"]
