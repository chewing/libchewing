language: c

compiler: gcc
env: BUILD=autotools

matrix:
  include:
    - compiler: clang
      env: BUILD=autotools CONFIGURE_OPTION=--disable-binary-data
    - compiler: gcc
      env: BUILD=cmake
    - compiler: clang
      env: BUILD=cmake CMAKE_OPTION=-DUSE_VALGRIND=no CFLAGS=-fsanitize=address

script:
  - if [ x$BUILD == xautotools ]; then ./autogen.sh && ./configure $CONFIGURE_OPTION && make && make check; fi
  - if [ x$BUILD == xcmake ]; then cmake . $CMAKE_OPTION && make && make test; fi

before_install:
  - sudo add-apt-repository --yes ppa:czchen/travis-ci # For cmake 2.8.9
  - sudo apt-get update -qq
  - sudo apt-get install -qq texinfo cmake valgrind
